a = [1, 2, 43]
b = [1, 2, 43]
#python creates a new object in memory for same lists also, so they are different objects

print(a is b)  # exact location of object in memory
print(a == b)  # value of object

